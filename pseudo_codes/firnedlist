class FriendList{
private:
	vector<User> friends;
public:
	FriendList()
	void newFriend(User);
	void removeFriend(User);
	vector<User> getFriends();
	bool isFriend(User);

}

FriendList::FriendList(){
	vector<User> friends;
}

FriendList::newFriend(User){
	friends.pushback(User);
}

FriendList::removeFriend(User){
	if isFriend(User){
		for loop until User index found in friends
			friends.erase(index)
		}
	else{
		output "not friend"
	}
}

FriendList::isFriend(User){
	//REVOIR LE COURS TODO
}

FriendList::getFriends(){
	return friends;
}